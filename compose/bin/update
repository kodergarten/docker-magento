#!/bin/bash
mkdir -p tmpupdate && cd $_
curl -s https://raw.githubusercontent.com/markshust/docker-magento/nossl/lib/template | bash
rm -rf .git
rsync -av ./ ../
cd ..
rm -rf tmpupdate
echo "docker-magento has been updated, run bin/restart to apply the updates"
